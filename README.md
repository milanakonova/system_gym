# Система управления тренажерным залом (Gym Management System)

Веб-приложение для управления тренажерным залом, построенное на FastAPI и PostgreSQL.

## Возможности

- 👥 Управление клиентами и пользователями
- 📅 Бронирование тренировок и расписание
- 💼 Управление контрактами и абонементами
- 🏋️ Управление залами и зонами
- 🔐 Система шкафчиков
- 👨‍🏫 Расписание тренеров
- 📊 Учет посещений
- 🔑 Аутентификация и авторизация (JWT)

## Технологии

- **Backend**: FastAPI (Python 3.11+)
- **База данных**: PostgreSQL 12+
- **Аутентификация**: JWT (python-jose)
- **ORM**: SQLAlchemy 2.0

## Быстрый старт

### Локальная разработка

1. **Клонирование репозитория**
   ```bash
   git clone <repository_url>
   cd system_gym
   ```

2. **Создание виртуального окружения**
   ```bash
   python3 -m venv venv
   source venv/bin/activate  # Linux/Mac
   # или
   venv\Scripts\activate  # Windows
   ```

3. **Установка зависимостей**
   ```bash
   pip install -r requirements.txt
   ```

4. **Настройка базы данных**
   - Установите PostgreSQL
   - Создайте базу данных и пользователя
   - Скопируйте `env.example` в `.env` и настройте переменные окружения
   ```bash
   cp env.example .env
   nano .env  # Отредактируйте DATABASE_URL
   ```

5. **Инициализация базы данных**
   ```bash
   python init_database.py
   ```

6. **Запуск приложения**
   ```bash
   # Вариант 1: Используя скрипт (Linux/Mac)
   ./start.sh
   
   # Вариант 2: Напрямую
   uvicorn scr.main:app --reload
   ```

7. **Доступ к приложению**
   - API: http://localhost:8000
   - Документация Swagger: http://localhost:8000/api/docs
   - ReDoc: http://localhost:8000/api/redoc

### Данные для входа по умолчанию

После инициализации базы данных создается администратор:
- **Email**: admin@gym.com
- **Password**: admin123

⚠️ **ВАЖНО**: Измените пароль после первого входа!

## Развертывание на сервере

Подробная инструкция по развертыванию на сервере находится в файле [DEPLOYMENT.md](DEPLOYMENT.md).

### Краткая инструкция:

1. **Подготовка сервера** (установка Python, PostgreSQL)
2. **Клонирование проекта** в `/opt/system_gym`
3. **Настройка базы данных** и переменных окружения
4. **Инициализация БД**: `python init_database.py`
5. **Настройка systemd** для автозапуска (см. `system_gym.service`)
6. **Опционально**: Настройка Nginx как reverse proxy

### Docker развертывание

```bash
# Настройте переменные в .env
cp env.example .env
nano .env

# Запуск
docker-compose up -d

# Инициализация БД
docker-compose exec app python init_database.py
```

## Структура проекта

```
system_gym/
├── scr/                    # Исходный код приложения
│   ├── api/               # API эндпоинты
│   ├── core/              # Основные настройки и утилиты
│   ├── db/                # Модели и работа с БД
│   ├── schemas/           # Pydantic схемы
│   ├── services/          # Бизнес-логика
│   └── main.py           # Точка входа приложения
├── static/                # Статические файлы (CSS, JS)
├── templates/             # HTML шаблоны
├── init_database.py      # Скрипт инициализации БД
├── requirements.txt      # Python зависимости
├── docker-compose.yml    # Docker Compose конфигурация
├── Dockerfile            # Docker образ приложения
├── system_gym.service    # Systemd unit файл
├── start.sh              # Скрипт запуска
└── DEPLOYMENT.md         # Подробная инструкция по развертыванию
```

## API Документация

После запуска приложения документация доступна по адресам:
- **Swagger UI**: `/api/docs`
- **ReDoc**: `/api/redoc`

## Переменные окружения

Основные переменные окружения (см. `env.example`):

- `DATABASE_URL` - строка подключения к PostgreSQL
- `SECRET_KEY` - секретный ключ для JWT (обязательно измените!)
- `CORS_ORIGINS` - разрешенные домены для CORS
- `HOST` - хост для запуска (по умолчанию 0.0.0.0)
- `PORT` - порт (по умолчанию 8000)
- `RELOAD` - автоперезагрузка при изменении кода (False для production)

## Разработка

### Добавление новых зависимостей

```bash
pip install <package>
pip freeze > requirements.txt
```

### Запуск тестов

```bash
# Добавьте тесты в проект и запустите
pytest
```

## Лицензия

[Укажите лицензию]

## Поддержка

При возникновении проблем:
1. Проверьте логи приложения
2. Убедитесь, что PostgreSQL запущен и доступен
3. Проверьте настройки в `.env`
4. См. раздел "Устранение неполадок" в [DEPLOYMENT.md](DEPLOYMENT.md)
